# =============================================================================
# Day 3 (Lab 3) 환경 변수 예시
#
#   cp ~/docker-labs-3days/assets/lab03/.env.example ~/lab03/.env
#
# 실제 값이 담긴 .env 는 Git 에 올리지 않습니다. 이 예시 파일만 저장소에 둡니다.
# 실습용 값이므로 그대로 쓰셔도 되지만, 실무에서는 반드시 바꾸십시오.
# =============================================================================

# --- postgres:16 컨테이너 초기화용 (DB·계정을 만들 때 사용) ---
POSTGRES_USER=lab
POSTGRES_PASSWORD=labpass
POSTGRES_DB=labdb

# --- Flask 앱이 DB 에 접속할 때 사용 ---
# DB_HOST 는 compose.yaml 의 environment 에 있습니다 (비밀이 아니라 구조 값).
DB_NAME=labdb
DB_USER=lab
DB_PASSWORD=labpass
